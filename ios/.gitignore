App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated Config files
App/App/capacitor.config.json
App/App/config.xml

# Regenerated by `cap sync ios` from the local node_modules path - committing
# it bakes in an absolute path from whichever machine last ran sync, which
# breaks on every other machine.
App/CapApp-SPM/symlinks
